# Triaged Python advisories (Sprint 8, T8.6).
#
# One advisory id per line, followed by ` # owner — reason — review-by YYYY-MM-DD`.
# A vulnerability listed here does NOT fail CI. Nothing may sit here without an
# owner and a review date; the security review
# (docs/guides/security-checklist.md) re-reads this file every sprint and
# removes stale entries.
#
# Sprint 8 sweep result: 20 vulnerable packages found, 18 upgraded in uv.lock
# (75 advisories → 15). The two below could not be resolved to their fixed
# versions at lock time — `uv lock --upgrade-package aiohttp --upgrade-package
# pynacl --refresh-package aiohttp --refresh-package pynacl` still selects the
# versions listed here. Both are transitive (aiohttp via aiogram/discord.py/
# twilio/aiogoogle; PyNaCl via discord.py voice), so the fix arrives with an
# upstream release rather than a direct bump. Dependabot will open the PR;
# these entries come out the moment it does.

# ── aiohttp 3.13.5 → 3.14.3 (transitive; not resolvable at lock time) ──
# owner: @vpu — review-by 2026-09-17
PYSEC-2026-237
PYSEC-2026-2104
PYSEC-2026-2105
PYSEC-2026-2106
PYSEC-2026-2107
PYSEC-2026-2108
PYSEC-2026-2109
PYSEC-2026-2110
PYSEC-2026-2111
PYSEC-2026-2112
PYSEC-2026-2113
PYSEC-2026-3545
PYSEC-2026-3546
PYSEC-2026-3547

# ── PyNaCl 1.5.0 → 1.6.2 (transitive via discord.py voice) ──
# owner: @vpu — review-by 2026-09-17
PYSEC-2026-3002

# ── anyio 4.13.0 → 4.14.2 (blocked: breaks MCP stdio teardown) ──
# owner: @vpu — review-by 2026-10-03
# `uv lock --upgrade-package anyio` resolves 4.14.2, but 4.14 enforces that a
# cancel scope exits in the task that entered it, and our MCP manager/client
# tear `stdio_client` down from another task: 5 tests in
# tests/test_mcp_manager_integration.py fail ("Attempted to exit cancel scope in
# a different task"), and mcp 1.30 on top of it hangs. The upgrade lands with the
# MCP lifecycle fix (enter and exit each server's stack in one owner task).
# Exposure meanwhile:
#  - CVE-2026-63374 (GHSA-82r6-8w77-94w6): IDNA-2003 cert confusion in
#    connect_tcp/TLSStream. Reachable via httpx's anyio backend, but only for
#    non-ASCII host names on an already-hijacked connection; we dial none.
#  - CVE-2026-64847 (GHSA-5p39-cfhj-2xmp): undrained stderr in anyio's
#    process-pool workers. We never use anyio.to_process.
CVE-2026-63374
CVE-2026-64847
